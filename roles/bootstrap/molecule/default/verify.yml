- name: Verify
  hosts: all
  tasks:
    - name: test connection
      ansible.builtin.ping:

    - name: try the package module
      ansible.builtin.package:
        name: gzip
        state: present
